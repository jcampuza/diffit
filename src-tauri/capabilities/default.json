{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for every repository window",
  "windows": ["main", "repo-*"],
  "permissions": ["core:default", "dialog:allow-open"]
}
